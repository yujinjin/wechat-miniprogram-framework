<view class="auth-box">
    <button wx:if="{{!isLogin}}" loading="{{isSumbiting}}" class="auth-button" open-type="getUserInfo" lang="zh_CN" bindgetuserinfo="authUserTap"></button>
    <slot wx:else></slot>
</view>
